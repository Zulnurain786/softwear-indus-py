# 1. Show both containers running
docker ps

# 2. Show Flask app works
curl http://localhost:8080

# 3. Show MySQL time endpoint (THE KEY DELIVERABLE)
curl http://localhost:8080/mysql-time

# 4. Show logs
docker logs mysite

# 5. Show inspect (network, ports, config)
docker inspect mysite

# 6. Show image layers
docker history myapp